<form method="post" action="{{ path('app_admincategorie_delete', {'id': categorie.id}) }}" onsubmit="tu es sure que tu veux supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.id) }}">
    <button class="btn btn-dark">Supprimer</button>
</form>
