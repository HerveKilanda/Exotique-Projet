{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_categorie_index') }}">Retour a la liste des catégories</a>

    <a href="{{ path('app_admincategorie_edit', {'id': categorie.id}) }}">Modifier la categorie</a>

    {{ include('admincategorie/_delete_form.html.twig') }}
{% endblock %}
